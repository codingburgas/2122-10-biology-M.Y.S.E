#include "front-end.h"

int main()
{
    /*
    sf::RenderWindow window{ {1920, 990}, "Ecosystem Symolation" };

    tgui::Gui gui{ window };

    if (runExample(gui))
        gui.mainLoop();
    */

    simulation();
}